"""
도서관 무인예약 확인 핵심 로직 패키지.

원본: D:\\workspace\\sblib-search (서울시립도서관 통합검색 무인예약 확인 CLI)
이 패키지는 원본 프로젝트의 src/*.py 핵심 로직(입력 로드, 검색, HTML 파싱,
상태 판정, 속도 제한, 재시도)을 그대로 옮긴 것입니다.

원본과의 차이점:
- 원본은 서로 "bare" 임포트(`from errors import ...`)를 사용하고 테스트
  인프라가 sys.path에 src/를 추가해서 지원했습니다. 여기서는 일반적인
  패키지 상대 임포트(`from .errors import ...`)로 정리했습니다.
- 로직 자체(파싱 규칙, 재시도/속도제한 정책, 상태 판정 규칙)는 변경하지
  않았습니다.
"""
